import Modal from "./Modal";
import { RiDeleteBin6Line } from "react-icons/ri";



const DeleteModal = ({ close, action, title, isOpen }) => {
    return (
        <Modal closeModal={close} isOpen={isOpen} width={'max-w-2xl'}>
       
        </Modal>
    )
}


export default DeleteModal